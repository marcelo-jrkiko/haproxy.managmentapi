# Minimal rsyslog config for HAProxy in container
module(load="imuxsock")

# Keep processing simple inside containers.
$RepeatedMsgReduction off

# Write HAProxy access logs to a file and container stdout.
local0.*    /var/log/access.log
local0.*    /proc/1/fd/1

# Prevent duplicate handling by default rules.
& stop
